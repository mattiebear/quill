import { chakra } from '@chakra-ui/react';
import {
	HomeIcon as HomeIconBase,
	UserIcon as UserIconBase,
} from '@heroicons/react/24/outline';

export const HomeIcon = chakra(HomeIconBase);
export const UserIcon = chakra(UserIconBase);
